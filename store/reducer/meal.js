import { MEALS } from "../../data/dummy-data";
const init = {
    meals: MEALS,
    filterMeals: MEALS,
    favMeals: [],
};

const reducer = (state = init, action) => {
    return state;
};

export default reducer;
